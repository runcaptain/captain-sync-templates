# Local Terraform state and provider cache (never commit).
.terraform/
.build/
*.tfstate
*.tfstate.*
crash.log
# Filled-in vars carry the one-time secret. Only the .example is committed.
terraform.tfvars
*.auto.tfvars
# Python bytecode from zipping the Lambda sources.
functions/__pycache__/
