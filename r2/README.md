# Captain deploy: Cloudflare R2

Coming next, see CAP-572.

Same shape as `../aws/cloudformation`: event wiring (R2 event notifications), a
read-only cross-account grant Captain uses, and a self-verifying phone-home so a
green deploy is a confirmed deploy.
