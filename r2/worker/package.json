{
  "name": "captain-r2-sync-worker",
  "version": "2026.8.13",
  "private": true,
  "description": "Captain R2 sync Worker: queue consumer + keyless read proxy + event-path self-test.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format pretty"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260811.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
